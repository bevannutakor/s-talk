#include "list.h"
//need to read input then call send signaller
//when input is entered put it in the linked list and call send

void messageInputCreateThread(List* list);
void messageInputCancelThread();
void messageInputFinishThread();
